# 🚀 Инструкция по деплою на VPS

Пошаговая инструкция для деплоя Telegram RAG бота на любой VPS сервер.

## 📋 Требования к серверу

### Минимальные характеристики

- **CPU:** 2 ядра
- **RAM:** 4 GB
- **Диск:** 20 GB SSD
- **ОС:** Ubuntu 22.04 LTS (рекомендуется) или Debian 11+

### Рекомендуемые провайдеры

- [DigitalOcean](https://www.digitalocean.com/) — $24/месяц (4GB RAM)
- [Hetzner](https://www.hetzner.com/) — €5/месяц (4GB RAM)
- [Timeweb](https://timeweb.com/) — от 400₽/месяц
- [Selectel](https://selectel.ru/) — от 500₽/месяц

## 🔧 Подготовка сервера

### 1. Подключение к серверу

```bash
ssh root@ваш-ip-адрес
```

### 2. Обновление системы

```bash
apt update && apt upgrade -y
```

### 3. Установка Docker

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Запуск Docker
systemctl start docker
systemctl enable docker

# Проверка установки
docker --version
```

### 4. Установка Docker Compose

```bash
# Установка Docker Compose v2
apt install docker-compose-plugin -y

# Проверка установки
docker compose version
```

### 5. Установка Git

```bash
apt install git -y
```

### 6. Создание пользователя (опционально, для безопасности)

```bash
# Создаём нового пользователя
adduser appuser

# Добавляем в группу docker
usermod -aG docker appuser

# Переключаемся на пользователя
su - appuser
```

## 📦 Деплой приложения

### 1. Клонирование репозитория

```bash
cd ~
git clone <ваш-репозиторий-url>
cd <название-папки>
```

### 2. Настройка переменных окружения

```bash
# Копируем пример
cp .env.example .env

# Редактируем конфигурацию
nano .env
```

**Заполните обязательные поля:**

```env
OPENAI_API_KEY=sk-proj-ваш-реальный-ключ
TELEGRAM_BOT_TOKEN=ваш-реальный-токен-бота
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

### 3. Запуск приложения

```bash
# Запуск в фоновом режиме
docker compose up -d --build
```

### 4. Проверка статуса

```bash
# Проверяем запущенные контейнеры
docker compose ps

# Смотрим логи бота
docker compose logs -f bot

# Смотрим логи админки
docker compose logs -f admin
```

## 🔥 Настройка Firewall

### UFW (Ubuntu Firewall)

```bash
# Включаем firewall
ufw enable

# Разрешаем SSH
ufw allow 22/tcp

# Разрешаем админ-панель (если нужен внешний доступ)
ufw allow 8501/tcp

# Проверяем статус
ufw status
```

⚠️ **Важно:** Если админка нужна только локально, не открывайте порт 8501.

## 🌐 Доступ к админ-панели

### Вариант 1: SSH туннель (рекомендуется)

На локальной машине:

```bash
ssh -L 8501:localhost:8501 root@ваш-ip-адрес
```

Затем откройте в браузере: [http://localhost:8501](http://localhost:8501)

### Вариант 2: Прямой доступ (небезопасно)

Если открыли порт 8501 в firewall:

```
http://ваш-ip-адрес:8501
```

⚠️ **Внимание:** Используйте Nginx с базовой аутентификацией или VPN для защиты админки.

## 📊 Мониторинг

### Просмотр логов

```bash
# Все логи
docker compose logs

# Логи конкретного сервиса в реальном времени
docker compose logs -f bot
docker compose logs -f admin
docker compose logs -f qdrant

# Последние 100 строк
docker compose logs --tail=100 bot
```

### Проверка использования ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Использование диска
df -h

# Использование памяти
free -h
```

### Проверка статуса сервисов

```bash
# Статус всех сервисов
docker compose ps

# Проверка Qdrant
curl http://localhost:6333/healthz

# Количество документов в Qdrant
curl http://localhost:6333/collections/knowledge_base
```

## 🔄 Обновление приложения

### 1. Получение новых изменений

```bash
cd ~/ваш-проект
git pull origin main
```

### 2. Перезапуск с обновлением

```bash
# Пересборка и перезапуск
docker compose up -d --build

# Или только перезапуск без пересборки
docker compose restart
```

### 3. Обновление только бота

```bash
docker compose up -d --build --no-deps bot
```

## 🛠️ Обслуживание

### Очистка старых образов и контейнеров

```bash
# Удаление неиспользуемых образов
docker image prune -a

# Полная очистка системы Docker
docker system prune -a --volumes
```

### Backup данных Qdrant

```bash
# Создание backup
docker compose exec qdrant tar czf /qdrant/storage/backup.tar.gz /qdrant/storage

# Копирование на локальную машину
docker cp qdrant:/qdrant/storage/backup.tar.gz ./qdrant-backup.tar.gz
```

### Восстановление из backup

```bash
# Остановка сервисов
docker compose down

# Очистка volume
docker volume rm <project>_qdrant_data

# Запуск с новым volume
docker compose up -d qdrant

# Копирование backup в контейнер
docker cp ./qdrant-backup.tar.gz qdrant:/tmp/

# Распаковка
docker compose exec qdrant tar xzf /tmp/backup.tar.gz -C /

# Перезапуск
docker compose restart qdrant
```

## 🔐 Безопасность

### 1. Использование secrets для токенов

Измените `docker-compose.yml`:

```yaml
services:
  bot:
    secrets:
      - telegram_token
      - openai_key
    environment:
      - TELEGRAM_BOT_TOKEN_FILE=/run/secrets/telegram_token
      - OPENAI_API_KEY_FILE=/run/secrets/openai_key

secrets:
  telegram_token:
    file: ./secrets/telegram_token.txt
  openai_key:
    file: ./secrets/openai_key.txt
```

### 2. Настройка автообновлений системы

```bash
apt install unattended-upgrades -y
dpkg-reconfigure --priority=low unattended-upgrades
```

### 3. Настройка fail2ban

```bash
apt install fail2ban -y
systemctl enable fail2ban
systemctl start fail2ban
```

## 📈 Автозапуск при перезагрузке

Docker Compose автоматически настроен с `restart: always`, поэтому сервисы запустятся после перезагрузки сервера.

Проверка:

```bash
# Перезагрузка сервера
reboot

# После подключения проверяем
docker compose ps
```

## 🚨 Troubleshooting

### Контейнеры не запускаются

```bash
# Проверяем логи
docker compose logs

# Проверяем конфигурацию
docker compose config

# Пересоздаём контейнеры
docker compose down
docker compose up -d --force-recreate
```

### Нет места на диске

```bash
# Проверяем использование
df -h

# Очищаем Docker
docker system prune -a --volumes

# Очищаем логи
truncate -s 0 /var/lib/docker/containers/*/*-json.log
```

### Проблемы с памятью

```bash
# Проверяем использование
free -h

# Ограничиваем память контейнеров в docker-compose.yml
services:
  bot:
    mem_limit: 1g
  admin:
    mem_limit: 1g
```

### Медленная работа

1. Проверьте использование CPU: `htop`
2. Проверьте скорость диска: `iostat -x 1`
3. Увеличьте ресурсы сервера
4. Оптимизируйте параметры в `.env`:
   - Уменьшите `RAG_TOP_K`
   - Увеличьте `RAG_SCORE_THRESHOLD`

## 📞 Поддержка

### Полезные команды

```bash
# Вся информация о системе
docker info

# Версии всех компонентов
docker version
docker compose version
python --version

# Использование портов
netstat -tulpn | grep LISTEN

# Проверка DNS
nslookup api.openai.com
```

### Логи системы

```bash
# Системные логи
journalctl -xe

# Логи Docker
journalctl -u docker.service

# Логи конкретного контейнера
docker logs <container-id>
```

## ✅ Чеклист успешного деплоя

- [ ] Сервер настроен и обновлён
- [ ] Docker и Docker Compose установлены
- [ ] Репозиторий склонирован
- [ ] Файл `.env` настроен с правильными ключами
- [ ] Приложение запущено: `docker compose ps` показывает все сервисы как `Up`
- [ ] Логи бота не показывают ошибок
- [ ] Бот отвечает на команду `/start` в Telegram
- [ ] Админка доступна и можно загрузить файл
- [ ] Данные успешно проиндексированы
- [ ] Бот отвечает на вопросы по базе знаний
- [ ] Настроен firewall
- [ ] Настроен мониторинг логов

---

**Готово!** Ваш бот работает в production режиме 24/7. 🎉
