# 🚀 Руководство по развертыванию (Deployment Guide)

Это руководство описывает процесс развертывания RAG-агента (Telegram-бот + Админ-панель + Qdrant) на Linux-сервере (Ubuntu/Debian).

## 📋 Предварительные требования

Перед началом убедитесь, что у вас есть:

1.  **VPS/Выделенный сервер**:
    *   **ОС**: Ubuntu 20.04 LTS или 22.04 LTS (рекомендуется).
    *   **CPU**: Минимум 2 ядра.
    *   **RAM**: Минимум 4 ГБ (Qdrant и ML-библиотеки могут потреблять много памяти).
    *   **Disk**: 20 ГБ+ свободного места.
2.  **Доступы**:
    *   SSH-доступ к серверу с правами `root` или `sudo`.
    *   API Key от OpenAI.
    *   Токен Telegram-бота (от @BotFather).

---

## 💻 Шаг 0: Подготовка репозитория (на локальной машине)

Если вы еще не загрузили код на GitHub, выполните следующие действия на своем компьютере:

1.  **Инициализация Git**:
    ```bash
    git init
    ```

2.  **Создание .gitignore**:
    Убедитесь, что файл `.gitignore` существует и содержит исключения для системных файлов и секретов (например, `.env`, `__pycache__`, `venv/`).

3.  **Добавление файлов и коммит**:
    ```bash
    git add .
    git commit -m "Initial commit"
    ```

4.  **Создание репозитория на GitHub**:
    *   Зайдите на [GitHub](https://github.com/new).
    *   Создайте новый репозиторий (Public или Private).
    *   Скопируйте URL репозитория (например, `https://github.com/username/repo.git`).

5.  **Отправка кода**:
    ```bash
    git branch -M main
    git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
    git push -u origin main
    ```

---

## 🛠 Шаг 1: Подготовка сервера

Обновите список пакетов и установите необходимые утилиты:

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y git curl
```

### Установка Docker и Docker Compose

Если Docker еще не установлен, выполните следующую команду (официальный скрипт установки):

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

Убедитесь, что Docker запущен и добавлен в автозагрузку:

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

Добавьте текущего пользователя в группу docker (чтобы не писать `sudo` каждый раз):

```bash
sudo usermod -aG docker $USER
# После этой команды нужно перелогиниться (выйти и зайти по SSH)
```

---

## 📦 Шаг 2: Установка проекта

1.  **Клонируйте репозиторий** в папку `/opt` или домашнюю директорию:

    ```bash
    cd ~
    git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> demiand-test

    cd demiand-test

    ```

2.  **Создайте файл конфигурации**:

    Скопируйте пример конфигурации:

    ```bash
    cp .env.example .env
    ```

3.  **Редактирование .env**:

    Откройте файл `.env` в редакторе (например, `nano`):

    ```bash
    nano .env
    ```

    Заполните следующие значения:

    ```ini
    OPENAI_API_KEY=sk-proj-...          # Ваш ключ OpenAI
    TELEGRAM_BOT_TOKEN=123456:ABC...    # Токен вашего бота
    QDRANT_URL=http://qdrant:6333       # Оставьте как есть для Docker
    QDRANT_API_KEY=                     # Можно оставить пустым для локальной сети Docker
    COLLECTION_NAME=knowledge_base      # Имя коллекции (можно оставить по умолчанию)
    ```

    *Нажмите `Ctrl+O`, `Enter` для сохранения и `Ctrl+X` для выхода.*

---

## 🚀 Шаг 3: Запуск приложения

Запустите все сервисы в фоновом режиме с помощью Docker Compose:

```bash
docker compose up --build -d
```

*Примечание: Если у вас старая версия Docker Compose, используйте команду `docker-compose` вместо `docker compose`.*

### Проверка статуса

Убедитесь, что все контейнеры запущены (должно быть 3 контейнера: `qdrant`, `admin_ui`, `bot`):

```bash
docker compose ps
```

Посмотреть логи (если что-то не работает):

```bash
docker compose logs -f
```

---

## 🌐 Шаг 4: Доступ к сервисам

После успешного запуска сервисы будут доступны по следующим адресам:

1.  **Админ-панель (Streamlit)**: `http://<IP-ВАШЕГО-СЕРВЕРА>:8501`
    *   Здесь вы можете загружать Excel-файлы для обновления базы знаний.
2.  **Telegram Бот**: Просто найдите вашего бота в Telegram и нажмите `/start`.
3.  **Qdrant Dashboard**: `http://<IP-ВАШЕГО-СЕРВЕРА>:6333/dashboard`

---

## 🛡️ Шаг 5: Безопасность (Рекомендуется)

По умолчанию порты 8501 и 6333 открыты для всего интернета. Для продакшн-среды рекомендуется закрыть их фаерволом или настроить Nginx с Basic Auth.

### Простая защита с UFW (Firewall)

Разрешите только SSH и необходимые порты (если не используете Nginx):

```bash
sudo ufw allow 22/tcp
sudo ufw allow 8501/tcp
sudo ufw enable
```

### (Опционально) Настройка Nginx + SSL (HTTPS)

Для защиты админ-панели паролем и использования HTTPS:

1.  Установите Nginx: `sudo apt install nginx`
2.  Настройте проксирование запросов с 80 порта на 8501.
3.  Используйте `certbot` для получения бесплатного SSL сертификата.

---

## 🔄 Обновление проекта

Чтобы обновить проект до последней версии кода:

```bash
cd ~/demiand-test
git pull
docker compose up --build -d
```

## ❓ Устранение неполадок (Troubleshooting)

### 1. Ошибка `permission denied` при запуске Docker
Если вы видите ошибку доступа к сокету Docker:
```bash
sudo chmod 666 /var/run/docker.sock
```
Или убедитесь, что ваш пользователь добавлен в группу `docker` (см. Шаг 1).

### 2. Конфликт портов (Port already in use)
Если порт 8501 или 6333 занят другим приложением:
1.  Откройте `docker-compose.yml`.
2.  Измените маппинг портов, например:
    ```yaml
    ports:
      - "8502:8501"  # Теперь админка будет на порту 8502
    ```
3.  Перезапустите контейнеры: `docker compose up -d`

### 3. Бот не отвечает
1.  Проверьте логи бота:
    ```bash
    docker compose logs bot
    ```
2.  Убедитесь, что токен `TELEGRAM_BOT_TOKEN` верный.
3.  Если видите ошибку `Unauthorized`, перевыпустите токен у @BotFather.

### 4. Ошибки OpenAI API
Если в логах `indexer` или `bot` есть ошибки связанные с OpenAI:
1.  Проверьте баланс на аккаунте OpenAI.
2.  Убедитесь, что `OPENAI_API_KEY` скопирован полностью и без лишних пробелов.
3.  Если вы используете модель `gpt-4` или `text-embedding-3-large`, убедитесь, что у вашего аккаунта есть доступ к этим моделям (Tier 1 и выше).

### 5. Qdrant не запускается
Если контейнер `qdrant` постоянно перезагружается:
1.  Проверьте логи: `docker compose logs qdrant`
2.  Возможно, не хватает оперативной памяти. Попробуйте добавить swap-файл или увеличить тариф VPS.
3.  Если база данных повреждена, удалите volume `qdrant_data` (см. раздел "Полная очистка") и перезапустите.

---

## 🗑️ Остановка и удаление

Остановить сервисы:
```bash
docker compose stop
```

Остановить и удалить контейнеры (данные в Qdrant сохранятся в volume):
```bash
docker compose down
```

Полная очистка (ВНИМАНИЕ: удалит базу знаний):
```bash
docker compose down -v
```
