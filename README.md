# 🤖 Ассистент по подбору аэрогрилей (RAG Agent)

Этот проект представляет собой систему на базе Python для создания базы знаний из Excel/CSV файлов и Telegram-бота, который отвечает на вопросы пользователей, используя эту базу знаний (RAG - Retrieval Augmented Generation).


## 🚀 Возможности

*   **Админ-панель (Streamlit)**: Простой веб-интерфейс для загрузки прайс-листов и спецификаций (Excel, CSV).
*   **Индексация (Qdrant)**: Автоматическая обработка файлов, очистка данных, генерация эмбеддингов (OpenAI `text-embedding-3-large`) и сохранение в векторную базу данных Qdrant.
*   **Telegram Бот (Aiogram + LangChain)**: Умный ассистент, который понимает естественный язык, ищет информацию в базе знаний и дает точные ответы о характеристиках товаров.

## 🛠 Технологии

*   **Python 3.9+**
*   **Streamlit**: Веб-интерфейс.
*   **Aiogram 3.x**: Асинхронный фреймворк для Telegram ботов.
*   **LangChain**: Фреймворк для работы с LLM и векторными базами.
*   **Qdrant**: Векторная база данных.
*   **OpenAI API**: Эмбеддинги и генерация ответов.
*   **Docker & Docker Compose**: Контейнеризация и оркестрация.

## 📦 Установка и запуск

### Предварительные требования

*   Установленный Docker и Docker Compose.
*   Ключ API от OpenAI.
*   Токен Telegram бота (от @BotFather).

### Шаг 1: Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd <папка-проекта>
```

### Шаг 2: Настройка окружения

Скопируйте файл `.env.example` в `.env` и заполните его своими данными:

```bash
cp .env.example .env
```

Откройте файл `.env` и укажите:

```env
OPENAI_API_KEY=sk-proj-...
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
QDRANT_URL=http://qdrant:6333  # Если запускаете через docker-compose, оставьте так
QDRANT_API_KEY=                # Если используете локальный Qdrant без ключа, оставьте пустым
```

### Шаг 3: Запуск через Docker Compose (Рекомендуется)

Это самый простой способ запустить все сервисы (Qdrant, Админку, Бота) одной командой.

```bash
docker-compose up --build -d
```

После запуска будут доступны:

*   **Админ-панель**: [http://localhost:8501](http://localhost:8501)
*   **Qdrant Dashboard**: [http://localhost:6333/dashboard](http://localhost:6333/dashboard)
*   **Telegram Бот**: Напишите вашему боту в Telegram.

### Шаг 4: Загрузка данных

1.  Откройте Админ-панель ([http://localhost:8501](http://localhost:8501)).
2.  Загрузите ваш Excel файл с характеристиками аэрогрилей.
3.  Нажмите "Обработать файл".
4.  Дождитесь сообщения об успехе.

Теперь бот готов отвечать на вопросы по загруженным данным!

## 🔧 Локальная разработка (без Docker)

Если вы хотите запустить проект локально для разработки:

1.  Создайте виртуальное окружение:
    ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/macOS
    # venv\Scripts\activate  # Windows
    ```

2.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```

3.  Запустите Qdrant (например, в докере):
    ```bash
    docker run -p 6333:6333 qdrant/qdrant
    ```

4.  Запустите Админку:
    ```bash
    streamlit run src/admin_ui.py
    ```

5.  Запустите Бота:
    ```bash
    python src/bot.py
    ```

## 📝 Структура проекта

*   `src/indexer.py`: Логика обработки файлов и работы с Qdrant.
*   `src/admin_ui.py`: Интерфейс на Streamlit.
*   `src/bot.py`: Логика Telegram бота.
*   `data/`: Папка для хранения данных (если нужно).
*   `Dockerfile`: Инструкции для сборки образа.
*   `docker-compose.yml`: Описание сервисов.
